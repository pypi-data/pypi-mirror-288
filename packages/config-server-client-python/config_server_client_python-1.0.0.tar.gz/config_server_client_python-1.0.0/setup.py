from setuptools import setup

setup(name='config-server-client-python',
      version='1.0.0',
      description='Package to fetch configurations from spring-cloud-config-server in Python',
      packages=['src'],
      author_email='jayantreddy181198@gmail.com',
      zip_safe=False)
