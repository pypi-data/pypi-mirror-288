import { LightPlugin } from '../index';
import { Vec2, Vec3 } from '../../mol-math/linear-algebra';
import { Structure } from '../../mol-model/structure/structure';
export declare const rotateArbitraryLine: (v1: any, v2: any, theta: number, point: any) => number[];
export declare const getDragOffset: (plugin: LightPlugin, start: Vec2, end: Vec2) => Vec3;
export declare const getBox: (structure: Structure, elementIds: number[]) => {
    center: Vec3;
    size: Vec3;
};
