import { LightPlugin } from '../index';
import { Representation } from '../../mol-repr/representation';
import { ModifiersKeys } from '../../mol-util/input/input-observer';
export interface RightClickEvent {
    loci: Representation.Loci;
    modifiers: ModifiersKeys;
    isSelection: boolean;
    reprType: string;
    isEmpty: boolean;
}
export declare class EventsManager {
    private ev;
    private allowEmptyClick;
    rightClick: import("rxjs").Subject<RightClickEvent>;
    plugin: LightPlugin;
    constructor(plugin: LightPlugin);
    subscribe(): void;
    setAllowEmptyClick(isAllow: boolean): void;
}
