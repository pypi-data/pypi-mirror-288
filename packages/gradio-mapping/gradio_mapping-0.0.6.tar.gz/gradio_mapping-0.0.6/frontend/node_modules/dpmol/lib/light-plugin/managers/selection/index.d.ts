import { LightPlugin } from '../../index';
import { ShapeSelectionManager } from './shape';
import { StructureSelectionManager } from './structure';
export declare class SelectionManager {
    structure: StructureSelectionManager;
    shape: ShapeSelectionManager;
    plugin: LightPlugin;
    private ev;
    readonly event: {
        changed: import("rxjs").Subject<{}>;
    };
    constructor(plugin: LightPlugin);
    clear(): void;
}
