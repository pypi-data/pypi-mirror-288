import { LightPlugin } from '../../index';
export declare class RotateEditor {
    plugin: LightPlugin;
    private state;
    private intervalTime;
    start(): Promise<void>;
    end(): void;
    constructor(plugin: LightPlugin);
}
