import { LightPlugin } from '../index';
import { Snapshot, SnapshotCell } from '../types';
export declare class SnapshotManager {
    private ev;
    plugin: LightPlugin;
    timestamp?: number;
    readonly event: {
        changed: import("rxjs").Subject<any>;
    };
    constructor(plugin: LightPlugin);
    setSnapshot(snapshot: Snapshot, getDataByServerData?: (serverData: any) => any): Promise<void>;
    getSnapshot(toServer?: boolean): Promise<{
        cells: SnapshotCell[];
        timestamp: number;
        params: {
            interactionParams: import("../types").InteractionParams;
            showPolarHydrogenOnly: boolean;
        };
    }>;
    refresh(): void;
    downloadAsImage(name: string, params?: {
        width: number;
        height: number;
    }): Promise<void>;
}
