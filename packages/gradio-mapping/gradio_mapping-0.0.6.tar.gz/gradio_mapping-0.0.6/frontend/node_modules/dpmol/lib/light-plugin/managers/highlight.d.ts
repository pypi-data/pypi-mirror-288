import { Representation } from '../../mol-repr/representation';
import { LightPlugin } from '../index';
export interface HighlightEvent {
    loci?: Representation.Loci;
    info?: any;
}
export declare class HighlightManager {
    plugin: LightPlugin;
    private ev;
    info: import("rxjs").Subject<HighlightEvent>;
    hoverLoci?: Representation.Loci;
    handleHighlightLocis: Representation.Loci[];
    constructor(plugin: LightPlugin);
    setHoverSelection(reprLoci?: Representation.Loci, isShowInfo?: boolean): void;
    addHighlightLocis(reprLocis: Representation.Loci[], isCover?: boolean): void;
    createHighlightInfo(reprLoci: Representation.Loci): {} | undefined;
    clear(): void;
}
