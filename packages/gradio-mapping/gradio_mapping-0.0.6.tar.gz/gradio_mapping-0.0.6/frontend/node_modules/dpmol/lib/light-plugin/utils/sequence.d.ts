import { Structure } from '../../mol-model/structure';
import { SecondaryStructureType } from '../../mol-model/structure/model/types';
export declare const getSequenceDataOfResidue: (structure: Structure, residue: any[], index: number) => {
    residueNum: any;
    residueName: any;
    standardResidueName: string;
    oneLetter: string;
    elementIds: any[];
    secondaryStructureType: string;
    secondaryStructureTypeInfo: SecondaryStructureType;
    secondaryStructureTypeKey: number;
    insCode: string;
    index: number;
};
