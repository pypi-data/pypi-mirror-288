services:
  postgres:
    container_name: container-pg
    image: postgres
    hostname: localhost
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: doadmin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: xplainable-local-test
    
    # Data persistence.
    #volumes:
    #  - postgres-data:/var/lib/postgresql/data
    
    restart: unless-stopped
