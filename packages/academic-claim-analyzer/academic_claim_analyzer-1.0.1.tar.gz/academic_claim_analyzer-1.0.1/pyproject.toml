[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "academic_claim_analyzer/_version.py"
version_scheme = "no-guess-dev"
local_scheme = "no-local-version"