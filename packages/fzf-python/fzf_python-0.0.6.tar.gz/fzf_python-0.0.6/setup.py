from setuptools import setup, find_packages

setup(
    name="fzf-python",
    version="0.0.6",
    packages=find_packages(),
    install_requires=[],
    long_description="fzfpy is a python wrapper for fzf",
)
