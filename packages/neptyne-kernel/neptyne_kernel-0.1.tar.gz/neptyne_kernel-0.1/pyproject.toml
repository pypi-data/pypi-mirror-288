[project]
name = "neptyne-kernel"
version = "0.1"
description = "The Neptyne kernel"
readme = "README.md"
authors = []
requires-python = "==3.11"
dynamic = ["dependencies"]
[tool.setuptools.dynamic]
dependencies = { file = ["kernel/requirements.txt"] }
