type: object
additionalProperties: false
required: [ light ]
properties:
  light:
    $ref: doc-badge-attr-logo
  dark:
    $ref: doc-badge-attr-logo
  size:
    type: string
    enum: [ "auto" ]
  width:
    type: string