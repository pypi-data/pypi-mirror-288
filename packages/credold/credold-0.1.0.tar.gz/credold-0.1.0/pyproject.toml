[tool.ruff]
target-version = "py312" # Pythonのバージョンを指定
fix = true               # 自動修正を有効にする
indent-width = 4         # インデントの幅を指定
src = ["src"]

line-length = 120

[tool.ruff.pydocstyle]
convention = "google" # docstringの形式を指定
