[project]
name = "donotation"
version = "0.0.1"
description = 'A library that introduces the Haskell-like do notation using a Python decorator.'
authors = [
  {name="Michael Schneeberger", email="michael.schneeb@hotmail.com"},
]
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
