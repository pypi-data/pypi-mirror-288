from setuptools import setup, find_packages

setup(
    name='ptmath',
    version='0.1.0', 
    description='Math and other funcs',
    url='https://github.com/funkaclau',
    author='funkaclau',
    packages=find_packages(),
    install_requires=[
    ],
)